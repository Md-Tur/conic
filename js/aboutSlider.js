var swiper = new Swiper(".mySwiper", {
    effect: "cards",
    grabCursor: true,
    // loop: true,
    // initialSlide: 0,
    // speed: 1000,
    // autoplay: {
    //     disableOnInteraction: false,
    // },
});